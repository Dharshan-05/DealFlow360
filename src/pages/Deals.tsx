import { useState } from "react"
import { AnimatePresence, motion } from "framer-motion"

type Signal = { level: "HIGH" | "MEDIUM" | "LOW"; title: string; impact: string; reason: string; action: string }
const signals: Signal[] = [
  { level: "HIGH", title: "Setup Service discount exceeds allowed limit.", impact: "Reduced margin and approval required.", reason: "18% discount exceeds the 10% allowed line-item limit.", action: "Review discount and submit approval justification." },
  { level: "MEDIUM", title: "Customer requested pricing change.", impact: "The quotation value and close timing may change.", reason: "Acme requested revised commercial terms this morning.", action: "Contact customer and record the requested change." },
  { level: "LOW", title: "Inventory availability declining.", impact: "Laptop allocation may delay fulfillment.", reason: "Only 4 units remain available at the East Depot.", action: "Reallocate inventory or offer an alternative product." },
]
const color = { HIGH: "#EF4444", MEDIUM: "#F59E0B", LOW: "#A1A1AA" }

export default function Deals() {
  const [selected, setSelected] = useState<Signal | null>(null)
  const [notice, setNotice] = useState("")
  return <div style={{ padding: 28, maxWidth: 1440, margin: "0 auto" }}><header style={{ marginBottom: 22 }}><h1 style={h1}>Deal Health</h1><p style={sub}>Monitor quotation, approval, customer, margin, inventory, and fulfillment signals in one place.</p></header>
    <div style={{ display: "grid", gridTemplateColumns: "310px 1fr", gap: 16, marginBottom: 16 }}><section className="df-card" style={{ padding: 20 }}><div style={label}>Overall deal health</div><div className="mono" style={{ color: "#fff", fontSize: 42, lineHeight: 1, fontWeight: 800, marginTop: 14 }}>78 <span style={{ color: "#555", fontSize: 17 }}>/ 100</span></div><div style={{ marginTop: 12 }}><Badge text="AT RISK" tone="#F59E0B"/></div><p style={{ color: "#888", fontSize: 12, lineHeight: 1.5, margin: "13px 0 0" }}>Composite signal score across commercial, approval, margin, inventory, customer, and fulfillment health.</p></section><section className="df-card" style={{ padding: 20 }}><div style={label}>Health breakdown</div><div style={{ display: "grid", gridTemplateColumns: "repeat(3,minmax(0,1fr))", gap: 13, marginTop: 13 }}>{[["Commercial Health", 82, "Healthy"], ["Approval Health", 64, "At Risk"], ["Margin Health", 71, "At Risk"], ["Inventory Health", 91, "Healthy"], ["Customer Health", 86, "Healthy"], ["Fulfillment Health", 78, "At Risk"]].map(([name, score, status]) => <div key={name as string}><div style={{ display: "flex", justifyContent: "space-between", color: "#aaa", fontSize: 11, gap: 5 }}><span>{name}</span><strong className="mono" style={{ color: "#fff" }}>{score}</strong></div><div style={{ height: 4, background: "#202020", marginTop: 7 }}><motion.div initial={{ width: 0 }} animate={{ width: `${score}%` }} style={{ height: "100%", background: status === "Healthy" ? "#10B981" : "#F59E0B" }}/></div><div style={{ marginTop: 5, fontSize: 10, color: status === "Healthy" ? "#10B981" : "#F59E0B" }}>{status}</div></div>)}</div></section></div>
    <div style={{ display: "grid", gridTemplateColumns: "1.15fr .85fr", gap: 16 }}><div style={{ display: "grid", gap: 16 }}><section className="df-card" style={{ overflow: "hidden" }}><div style={{ padding: "14px 16px", borderBottom: "1px solid #1a1a1a", color: "#fff", fontWeight: 600, fontSize: 13 }}>Risk Signals</div>{signals.map((signal) => <button key={signal.title} onClick={() => setSelected(signal)} style={{ width: "100%", textAlign: "left", border: 0, borderBottom: "1px solid #161616", background: "transparent", padding: "13px 16px", color: "inherit", cursor: "pointer" }}><div style={{ display: "flex", gap: 9, alignItems: "center" }}><Badge text={signal.level} tone={color[signal.level]}/><span style={{ color: "#eee", fontSize: 12, fontWeight: 600 }}>{signal.title}</span></div><div style={{ margin: "6px 0 0 72px", color: "#777", fontSize: 11 }}>{signal.impact} · View details</div></button>)}</section><section className="df-card" style={{ padding: 16 }}><div style={label}>Deal timeline</div><div style={{ marginTop: 13, display: "grid", gap: 11 }}>{[["Sep 01, 09:20", "Quotation Created", "M. Shah"], ["Sep 02, 13:45", "Discount Changed", "M. Shah"], ["Sep 03, 10:12", "Approval Requested", "System"], ["Sep 04, 15:18", "Customer Negotiation", "Rajesh Kumar"], ["Current", "Approval Completed", "Awaiting fulfillment"], ["Next", "Fulfillment Started", "—"]].map(([time, event, person], i) => <div key={event} style={{ display: "grid", gridTemplateColumns: "70px 12px 1fr", gap: 10, alignItems: "start" }}><span className="mono" style={{ color: "#666", fontSize: 10 }}>{time}</span><span style={{ width: 8, height: 8, borderRadius: 8, marginTop: 3, background: i === 4 ? "#7C3AED" : i < 4 ? "#10B981" : "#333" }}/><div style={{ color: i === 4 ? "#fff" : "#aaa", fontSize: 12, fontWeight: i === 4 ? 700 : 400 }}>{event}<span style={{ color: "#666", marginLeft: 7, fontSize: 11 }}>{person}</span></div></div>)}</div></section></div><div style={{ display: "grid", gap: 16 }}><section className="df-card" style={{ padding: 16 }}><div style={{ ...label, color: "#A78BFA" }}>Why is this deal at risk?</div><p style={{ color: "#ddd", fontSize: 13, lineHeight: 1.55, margin: "11px 0" }}>Discount variance on the Setup Service is the primary risk driver. Approval delay and reduced margin increase the overall risk.</p><ol style={{ margin: 0, paddingLeft: 18, color: "#999", fontSize: 12, lineHeight: 1.8 }}><li>Discount variance</li><li>Approval delay</li><li>Margin pressure</li></ol></section><section className="df-card" style={{ padding: 16 }}><div style={label}>Recommended actions</div><div style={{ display: "grid", gap: 8, marginTop: 12 }}>{["Review Discount", "Request Approval", "Contact Customer", "Reallocate Inventory", "Offer Alternative Product"].map((x) => <button key={x} className="df-btn-secondary" onClick={() => setNotice(`${x} added to the deal workflow.`)} style={{ textAlign: "left" }}>{x}</button>)}</div>{notice && <div style={{ color: "#10B981", fontSize: 11, marginTop: 10 }}>{notice}</div>}</section><section className="df-card" style={{ padding: 16 }}><div style={label}>Related entities</div><div style={{ color: "#aaa", fontSize: 12, lineHeight: 2, marginTop: 8 }}>Quotation <strong className="mono" style={{ color: "#fff" }}>Q-1042</strong><br/>Customer <strong style={{ color: "#fff" }}>Acme Corp</strong><br/>Approval <strong style={{ color: "#F59E0B" }}>Pending</strong><br/>Fulfillment <strong className="mono" style={{ color: "#fff" }}>ORD-2048</strong><br/>Subscription <strong className="mono" style={{ color: "#fff" }}>SUB-2041</strong><br/>Invoice <strong className="mono" style={{ color: "#fff" }}>INV-3012</strong></div></section></div></div>
    <AnimatePresence>{selected && <><motion.button onClick={() => setSelected(null)} aria-label="Close risk detail" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} style={scrim}/><motion.aside initial={{ x: 420 }} animate={{ x: 0 }} exit={{ x: 420 }} style={drawer}><div style={{ display: "flex", justifyContent: "space-between", padding: 20, borderBottom: "1px solid #202020" }}><Badge text={`${selected.level} RISK`} tone={color[selected.level]}/><button onClick={() => setSelected(null)} style={close}>×</button></div><div style={{ padding: 20 }}><h2 style={{ margin: 0, color: "#fff", fontSize: 16 }}>{selected.title}</h2>{[["Impact", selected.impact], ["Reason", selected.reason], ["Recommended Action", selected.action]].map(([k, v]) => <div key={k} style={{ marginTop: 18 }}><div style={label}>{k}</div><p style={{ color: "#ccc", fontSize: 13, lineHeight: 1.55, margin: "6px 0 0" }}>{v}</p></div>)}</div></motion.aside></>}</AnimatePresence>
  </div>
}
function Badge({ text, tone }: { text: string; tone: string }) { return <span style={{ display: "inline-flex", alignItems: "center", gap: 5, color: tone, border: `1px solid ${tone}35`, background: `${tone}14`, borderRadius: 4, padding: "3px 7px", fontSize: 10, fontWeight: 700 }}><span style={{ width: 5, height: 5, borderRadius: 5, background: tone }}/>{text}</span> }
const h1 = { margin: 0, color: "#fff", fontSize: 22, letterSpacing: "-.025em" }; const sub = { margin: "5px 0 0", color: "#555", fontSize: 13 }; const label = { color: "#666", fontSize: 10, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase" as const }; const scrim = { position: "fixed" as const, inset: 0, zIndex: 20, border: 0, background: "rgba(0,0,0,.62)" }; const drawer = { position: "fixed" as const, top: 0, right: 0, bottom: 0, zIndex: 21, width: "min(420px,100vw)", background: "#0b0b0b", borderLeft: "1px solid #252525" }; const close = { border: 0, background: "none", color: "#aaa", fontSize: 20, cursor: "pointer" }
