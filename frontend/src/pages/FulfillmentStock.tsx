import { useMemo, useState } from "react"
import { AnimatePresence, motion } from "framer-motion"

const stock = [
  { warehouse: "Main Warehouse", product: "Laptop Pro 14", inStock: 40, reserved: 18, status: "Available" },
  { warehouse: "East Depot", product: "Laptop Pro 14", inStock: 10, reserved: 6, status: "Low Stock" },
  { warehouse: "Main Warehouse", product: "Docking Station", inStock: 65, reserved: 12, status: "Available" },
]
const orders = [{ id: "ORD-2048", customer: "Acme Corp", status: "Pending", warehouses: "Main Warehouse", items: "Laptop Pro 14 ×2 · Docking Station ×1" }, { id: "ORD-2046", customer: "Beta Industries", status: "Reserved", warehouses: "East Depot", items: "Laptop Pro 14 ×4" }]
export default function FulfillmentStock() {
  const [query, setQuery] = useState("")
  const [warehouse, setWarehouse] = useState("All warehouses")
  const [selected, setSelected] = useState<typeof orders[number] | null>(null)
  const rows = useMemo(() => stock.filter(item => (warehouse === "All warehouses" || item.warehouse === warehouse) && `${item.warehouse} ${item.product} ${item.status}`.toLowerCase().includes(query.toLowerCase())), [warehouse, query])
  return <div style={{ padding: "28px", maxWidth: 1440, margin: "0 auto" }}><header style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-end", marginBottom: 22, gap: 16, flexWrap: "wrap" }}><div><h1 style={{ margin: 0, color: "#fff", fontSize: 22, letterSpacing: "-.025em" }}>Fulfillment &amp; Stock</h1><p style={{ margin: "5px 0 0", color: "#555", fontSize: 13 }}>Live stock per warehouse and every order needing fulfillment.</p></div><div style={{ color: "#10B981", fontSize: 12, fontWeight: 600 }}>● Stock synced</div></header><section className="df-card" style={{ overflow: "hidden", marginBottom: 16 }}><div style={{ padding: "14px 16px", borderBottom: "1px solid #1a1a1a", display: "flex", justifyContent: "space-between", gap: 10, flexWrap: "wrap" }}><span style={{ color: "#fff", fontSize: 13.5, fontWeight: 600 }}>Inventory availability</span><div style={{ display: "flex", gap: 7 }}><input aria-label="Search stock" className="df-input" value={query} onChange={event => setQuery(event.target.value)} placeholder="Search stock…" style={{ width: 180, height: 32, padding: "7px 9px", fontSize: 12 }}/><select aria-label="Warehouse filter" value={warehouse} onChange={event => setWarehouse(event.target.value)} style={{ background: "#0a0a0a", border: "1px solid #222", color: "#aaa", borderRadius: 6, fontSize: 12, padding: "0 8px" }}><option>All warehouses</option><option>Main Warehouse</option><option>East Depot</option></select></div></div><div style={{ overflowX: "auto" }}><table style={{ width: "100%", minWidth: 660, borderCollapse: "collapse" }}><thead><tr>{["Warehouse", "Product", "In stock", "Reserved", "Available", "Status"].map(label => <th key={label} style={{ color: "#444", fontSize: 10, textAlign: "left", textTransform: "uppercase", letterSpacing: ".07em", padding: "10px 16px", borderBottom: "1px solid #181818" }}>{label}</th>)}</tr></thead><tbody>{rows.map(item => <tr key={`${item.warehouse}-${item.product}`} className="table-row"><td style={{ padding: "12px 16px", color: "#fff", fontSize: 12.5, fontWeight: 600 }}>{item.warehouse}</td><td style={{ padding: "12px 16px", color: "#aaa", fontSize: 12 }}>{item.product}</td><td className="mono" style={{ padding: "12px 16px", color: "#ddd", fontSize: 12 }}>{item.inStock}</td><td className="mono" style={{ padding: "12px 16px", color: "#888", fontSize: 12 }}>{item.reserved}</td><td className="mono" style={{ padding: "12px 16px", color: item.inStock - item.reserved < 8 ? "#F59E0B" : "#fff", fontSize: 13, fontWeight: 700 }}>{item.inStock - item.reserved}</td><td style={{ padding: "12px 16px" }}><span style={{ color: item.status === "Low Stock" ? "#F59E0B" : "#10B981", fontSize: 11, fontWeight: 600 }}>{item.status}</span></td></tr>)}</tbody></table></div></section><section className="df-card" style={{ overflow: "hidden" }}><div style={{ padding: "14px 16px", borderBottom: "1px solid #1a1a1a", color: "#fff", fontSize: 13.5, fontWeight: 600 }}>Orders awaiting fulfillment</div><div style={{ overflowX: "auto" }}><table style={{ width: "100%", minWidth: 700, borderCollapse: "collapse" }}><thead><tr>{["Order", "Customer", "Status", "Warehouses", "Items", ""].map(label => <th key={label} style={{ color: "#444", fontSize: 10, textAlign: "left", textTransform: "uppercase", letterSpacing: ".07em", padding: "10px 16px", borderBottom: "1px solid #181818" }}>{label}</th>)}</tr></thead><tbody>{orders.map(order => <motion.tr key={order.id} onClick={() => setSelected(order)} className="table-row" whileHover={{ background: "rgba(255,255,255,.025)" }} style={{ cursor: "pointer", borderBottom: "1px solid #121212" }}><td className="mono" style={{ padding: "12px 16px", color: "#fff", fontSize: 12, fontWeight: 600 }}>{order.id}</td><td style={{ padding: "12px 16px", color: "#ddd", fontSize: 12 }}>{order.customer}</td><td style={{ padding: "12px 16px", color: order.status === "Pending" ? "#F59E0B" : "#10B981", fontSize: 12, fontWeight: 600 }}>{order.status}</td><td style={{ padding: "12px 16px", color: "#888", fontSize: 12 }}>{order.warehouses}</td><td style={{ padding: "12px 16px", color: "#888", fontSize: 12 }}>{order.items}</td><td style={{ padding: "12px 16px", color: "#777" }}>›</td></motion.tr>)}</tbody></table></div></section><AnimatePresence>{selected && <><motion.button aria-label="Close fulfillment detail" onClick={() => setSelected(null)} initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} style={{ position: "fixed", inset: 0, zIndex: 20, border: 0, background: "rgba(0,0,0,.58)" }}/><motion.aside initial={{ x: 420 }} animate={{ x: 0 }} exit={{ x: 420 }} transition={{ duration: .28 }} style={{ position: "fixed", zIndex: 21, top: 0, right: 0, bottom: 0, width: "min(420px,100vw)", overflowY: "auto", background: "#0b0b0b", borderLeft: "1px solid #252525" }}><div style={{ padding: 20, position: "sticky", top: 0, display: "flex", justifyContent: "space-between", background: "#0b0b0b", borderBottom: "1px solid #202020" }}><div><div className="mono" style={{ color: "#fff", fontWeight: 700 }}>{selected.id}</div><div style={{ color: "#777", fontSize: 12, marginTop: 4 }}>{selected.customer} · Q-1042</div></div><button onClick={() => setSelected(null)} style={{ color: "#aaa", background: "none", border: 0, fontSize: 18, cursor: "pointer" }}>×</button></div><div style={{ padding: 20, display: "grid", gap: 14 }}><Block title="Order items"><Line text="Laptop Pro 14 · Qty 2" value="Main Warehouse · Available 22 · Reserved 2"/><Line text="Docking Station · Qty 1" value="Main Warehouse · Available 53 · Pending"/></Block><Block title="Fulfillment workflow"><div style={{ color: "#aaa", fontSize: 12, lineHeight: 2 }}>● Pending<br/>○ Reserved<br/>○ Packed<br/>○ Shipped<br/>○ Delivered</div></Block><Block title="Activity"><div style={{ color: "#aaa", fontSize: 12, lineHeight: 2 }}>Order created · Sales system<br/>Warehouse assigned · Operations<br/>Stock check complete · System</div></Block><button className="df-btn-primary" style={{ padding: "10px" }}>Reserve Stock</button><button className="df-btn-secondary" style={{ padding: "10px" }}>Change Warehouse</button></div></motion.aside></>}</AnimatePresence></div>
}
function Block({ title, children }: { title: string; children: React.ReactNode }) { return <section className="df-card" style={{ padding: 15 }}><div style={{ color: "#555", fontSize: 10, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase", marginBottom: 10 }}>{title}</div>{children}</section> }
function Line({ text, value }: { text: string; value: string }) { return <div style={{ padding: "7px 0", borderBottom: "1px solid #191919" }}><div style={{ color: "#eee", fontSize: 12, fontWeight: 600 }}>{text}</div><div style={{ color: "#777", fontSize: 11, marginTop: 3 }}>{value}</div></div> }
